use("commerce")

db.produtos.updateMany(
    {},
    {}
)
use("commerce")
db.produtos.find({}, { nome: 1, tags: 1, _id: 0 });
use("commerce")
db.produtos.find(
    {ingredientes: {$size: 4}}
).count()